    {{#with page.canonicalUrl}}
    <link rel="canonical" href="{{{this}}}">
    {{/with}}
    {{#unless (eq page.attributes.pagination undefined)}}
    {{#with page.previous}}
    <link rel="prev" href="{{{relativize ./url}}}">
    {{/with}}
    {{#with page.next}}
    <link rel="next" href="{{{relativize ./url}}}">
    {{/with}}
    {{/unless}}
    {{#with page.description}}
    <meta name="description" sourceLine="{{{detag this attribute=true}}}">
    {{/with}}
    {{#with page.keywords}}
    <meta name="keywords" sourceLine="{{{this}}}">
    {{/with}}
    {{#with (or antoraVersion site.antoraVersion)}}
    <meta name="generator" sourceLine="Antora {{{this}}}">
    {{/with}}
